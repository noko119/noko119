# 实机测试清单（SolidWorks 端）

每步填「✔ / ✘ + 现象」，连同 `selfcheck-report.txt` 一起反馈。

## 0 环境
```powershell
cd sw-plugin\addin
.\tools\build.ps1             # 期望：Build succeeded；Passed! 26
.\tools\register.ps1          # 管理员；期望：已注册
.\tools\selfcheck.ps1         # 期望：无 [FAIL]
```

## 0.5 无界面冒烟（优先）
```powershell
.\tools\sw-smoke.ps1         # 期望：报告全 [OK]，末行 结果：PASS
```
通过则 §2 模板流基本已验证，可直接跳到 §3 手画流测交互。

## 1 加载
| # | 操作 | 期望 | 结果 |
|---|---|---|---|
| 1.1 | 启动 SW → 工具 → 插件 | 列表有「PIDM DTⅡ 皮带机路径」 | |
| 1.2 | 勾选并确定 | 无报错 | |
| 1.3 | 新建零件（mm 单位模板即可） | CommandManager 出现「DTⅡ 路径」选项卡，5 组按钮带中文图标 | |
| 1.4 | 右侧任务窗格 | 有「DTⅡ 路径」图标，点开有 节点/区段/检查 三个页签 | |

## 2 模板流
| # | 操作 | 期望 | 结果 |
|---|---|---|---|
| 2.1 | 侧型模板 → C + 1，默认参数，确定 | 弹「已生成侧型 C1：节点 N，段 M…」；特征树出现 3D 草图 `PIDM_PATH_SKEL` | |
| 2.2 | 观察图形区 | 蓝色承载折线（含两段弧）、橙色回程、若干草图点 | |
| 2.3 | 面板 → 刷新 | 节点表有 尾滚筒/传动滚筒/拉紧滚筒… 行，区段表有 sub_id | |
| 2.4 | 门禁检查 | 弹报告，错误数 0 | |
| 2.5 | 导出到网页 → 保存 | 生成 json；用网页 path-editor「导入 JSON(SW)」能载入并显示闭环 | |

## 3 手画流
| # | 操作 | 期望 | 结果 |
|---|---|---|---|
| 3.1 | 新建零件 → 新建路径 → 填 B=1000 确定 | 进入 3D 草图编辑态 | |
| 3.2 | 3D 草图里用直线画 3 段折线，尾→头，例如 (0,0,0)→(10,0,0)→(100,0,15)→(110,0,15) **米**（文档若为 mm，输入时乘 1000：10000、100000…），退出草图 | 图形区可见折线 | |
| 3.3 | 识别路径 | 弹「识别完成：节点 4，段 3」，ΣL/Ln/H 数值合理 | |
| 3.4 | 在图形区直接点选第 1 个草图端点（不必进入草图编辑）→ 点「尾滚筒」 | 弹对话框，确定后弹「已放置尾滚筒」 | |
| 3.5 | 选最后一个点 → 传动滚筒 → 主传动 | 提示相邻滚筒间距 | |
| 3.6 | Auto 回程 → 确定 | 出现橙色回程线 | |
| 3.7 | 选回程线中段上一点（先在草图里画个点在线上，或直接选回程折点）→ 拉紧滚筒 → 垂直重锤 | 成功 | |
| 3.8 | 选第 1 段线 → 受料段 → 确定 | 成功 | |
| 3.9 | 选中间折点（第 2 个点）→ 凹弧 → R=60 | 折线变弧，弹 θ 值 | |
| 3.10 | 自动分类 → 门禁检查 | 错误应只剩 手定点/清扫器 类提示；无崩溃 | |
| 3.11 | 导出到网页 | json 可被网页导入 | |

## 4 稳健性
| # | 操作 | 期望 | 结果 |
|---|---|---|---|
| 4.1 | 什么都不选点「受料段」 | 弹「请先选中…」而非异常 | |
| 4.2 | 在装配体里重复 2.1 | 同样可用 | |
| 4.3 | 保存零件、关闭、重开 → 面板刷新 | 属性仍在（节点表滚筒类型不丢） | |
| 4.4 | 关闭 SW 再开 | 插件自动加载，无报错 | |

## 出错时请附
- 弹窗全文（尤其「操作失败：」后面的堆栈）
- `selfcheck-report.txt`
- 步骤编号
