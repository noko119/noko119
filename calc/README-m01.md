# 本机计算（对着 DTⅡ PDF，给设计院引用）

权威分三层，不要混：

1. **手册原文**：载入你电脑上的 DTⅡ PDF，检索结果必须带页码原文  
2. **校对型谱**：对照手册抄进表格后才能推荐带宽（计算依据）  
3. **厂标扩展**：设计院/厂内约定，检索时标成「非手册」，不能冒充 DTⅡ  

用法：双击 `打开本机计算.bat` → 载入 PDF 建索引 → 检索「带宽 1000 托辊」→ 复制依据 → 需要计算时再填表点计算。
