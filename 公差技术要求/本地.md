# 本地工作目录（已锁定）

**工作目录：** `C:\ABC\My Work\DA Std`

Cursor：**File → Open Folder** → 选这个文件夹。  
以后公差 MD 都写在：

`C:\ABC\My Work\DA Std\公差技术要求\`

| 文件 | 作用 |
| --- | --- |
| `现行标注.md` | 尺寸公差、形位、技术要求 |
| `缺尺寸清单.md` | 缺项 |
| `工作口径.md` | 口径 |

聊天不作准，改这些文件。本地 Agent，不要 Cloud。

## 若该目录里还没有这些 MD

在**已经 pull 过本分支的仓库**里打开 PowerShell（路径有空格，必须加引号）：

```powershell
New-Item -ItemType Directory -Force -Path "C:\ABC\My Work\DA Std\公差技术要求" | Out-Null
Copy-Item -Force "公差技术要求\*.md" "C:\ABC\My Work\DA Std\公差技术要求\"
New-Item -ItemType Directory -Force -Path "C:\ABC\My Work\DA Std\.cursor\agents" | Out-Null
Copy-Item -Force ".cursor\agents\gdt-*.md" "C:\ABC\My Work\DA Std\.cursor\agents\"
```

若 `C:\ABC\My Work\DA Std` **本身就是** git 仓库 `noko119`：不要复制，在该目录执行：

```powershell
git fetch origin
git checkout cursor/gongcha-jishu-yaoqiu-ce8f
git pull origin cursor/gongcha-jishu-yaoqiu-ce8f
```

文件会出现在 `C:\ABC\My Work\DA Std\公差技术要求\`。

云端写不进你的 C 盘。拷完或 pull 完，用本地对话继续。
