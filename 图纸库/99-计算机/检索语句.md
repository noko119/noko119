---
tags:
  - 计算机
---

# 检索语句

不装插件：左上搜索图号，或点标签 `#托辊` `#现行`。

若安装 [Dataview](https://github.com/blacksmithgu/obsidian-dataview)，可在任意笔记粘贴：

```dataview
TABLE 图号, 名称, 图别, 材料, 版本, 状态, 部件
FROM "30-图纸卡片"
SORT 图号 ASC
```

只看现行托辊：

```dataview
TABLE 图号, 名称, 材料, 带宽, 辊径, 轴承, 内径
FROM "30-图纸卡片"
WHERE 部件 = "托辊" AND 状态 = "现行"
SORT 图号 ASC
```

待会签：

```dataview
TABLE 图号, 名称, 材料, 状态
FROM "30-图纸卡片"
WHERE 状态 = "修订中"
```
