---
type: 标注验收
图号:
名称:
图别:
材料:
版本: A
状态: 修订中
系列: DTⅡA
部件:
对照卡片:
对照pdf:
cad路径:
机械签:
工艺签:
计算机签:
结论: 未验收
tags:
  - 验收
  - 计算机
---

# 标注验收 · {{图号}}

存到 `99-计算机/验收/`，**文件名 = 图号**。日常优先把勾选写在图纸卡片正文；本表只在需要单独留痕时用。

六字段必须与标题栏、卡片 YAML 逐字相同：`图号` `名称` `图别` `材料` `版本` `状态`。

## 对照

- 卡片：`30-图纸卡片/{{图号}}.md`
- PDF：`80-附件/{{图号}}/{{图号}}.pdf`
- 规则：[[10-机械/图号规则]]
- 约定：[[99-计算机/工程图标注]]

## 机械

- [ ] 图号能按 [[10-机械/图号规则]] 拆开
- [ ] 图号 = 卡片文件名 = YAML「图号」= PDF 文件名
- [ ] 名称、图别与标题栏逐字相同

## 工艺

- [ ] 材料与标题栏逐字相同，能下料（部件/总图可为 `见明细`）
- [ ] 版本与工艺卡 / 改图单同号
- [ ] 未三勾不得标 `现行`

## 计算机

- [ ] 六字段无空项，与 PDF 标题栏逐字相同
- [ ] 附件夹有 PDF，**没有** `.slddrw`
- [ ] `cad路径` 已填；`git status` 无 `*.slddrw`

## 结论

- 全勾 → `结论: 通过`，卡片 `状态: 现行`
- 有叉 → `结论: 标错`，写错在哪一项，卡片保持 `修订中`
